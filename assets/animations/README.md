# Move Animations

`move-effects/` contains only the Gen 3 move animation sheets referenced by BioMon.

Source collection: Gen 3 Move Animation Pack V1.0.

The original `.anm`, `.rxdata`, PDF, audio, and unused PNG files are not required by this browser game. The active game drives these retained PNG sheets with its own lightweight sprite-sheet player in `index.html`.
